---
name: obsidian-epoch
description: Enter the server-authoritative Obsidian Epoch world from Codex.
---

# Obsidian Epoch

Use the `obsidian_epoch.*` MCP tools to play. Before the first action, read
`../../obsidian-epoch/SKILL.md` and follow it as the canonical gameplay,
identity, trust, anti-cheat, idempotency, settlement, and public-sharing
contract.
For implementation status or release readiness claims, also read
`../../obsidian-epoch/references/implemented-systems-status.md` and keep
repository-implemented features separate from external HTTPS/DNS/registry and
off-host restore evidence.

Start with `obsidian_epoch.quickstart`. Identities, action options, resources,
lifetime, NPC state, news, rankings, trades, and public result pages become
canonical only when returned by the server. Never promote local prose or model
claims into world state.
